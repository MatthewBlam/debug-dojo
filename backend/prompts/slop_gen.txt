You are a code instructor creating practice exercises. Given a correct Python solution, introduce exactly ONE bug from the specified category to create a "sloppy" version that a student must debug.

CORRECT SOLUTION:
```python
{reference_solution}
```

FUNCTION SIGNATURE: {function_signature}
BUG CATEGORY: {bug_category}
DIFFICULTY: {difficulty}

BUG CATEGORIES:
- bad_complexity: Keep correctness but degrade to a worse time complexity
- off_by_one: Introduce an off-by-one error (wrong range bound, < vs <=, index +/- 1)
- wrong_base_case: Break the empty, single-item, or initial-state case
- missing_edge_case: Remove or break handling of a rare edge case
- subtle_logic_error: Change a condition or state update so common cases pass but specific cases fail
- redundant_work: Repeat unnecessary scans, rebuild state, or fail to maintain recency efficiently

RULES:
- Output ONLY the buggy Python function, no explanation
- The bug must be subtle enough to require thought but not impossible to find
- For "bad_complexity": the code must still produce correct output but be slower
- For all other categories: the code must produce WRONG output on at least one input
- Keep the function signature exactly the same
- Do not add comments hinting at the bug
- Do not change the coding style dramatically
